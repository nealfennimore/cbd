<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "Christine's Birthday!" %></title>
    <%= stylesheet_link_tag "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <nav class="top-bar" data-topbar>
      <ul class="title-area">
        <li class="name">
          <h1><a href="/">Christine's Cake</a></h1>
        </li>
        <li class="toggle-topbar menu-icon"><a href="#"><span>Menu</span></a></li>
      </ul>

      <section class="top-bar-section">
        <ul class="right">
          <li class="has-dropdown">
            <a href="#"><% if current_user %>
                Welcome <strong><%= current_user.name %></strong>!</a>
                <ul class="dropdown">
                  <li><%= link_to "Sign out", signout_path, id: "sign_out" %></li>
                  <li><%= link_to "Show", user_path(current_user.id) %></li>
                  <li><%= link_to "Edit", edit_user_path(current_user.id) %></li>
                  <li><%= link_to 'Destroy', user_path(current_user.id), method: :delete, data: { confirm: 'Are you sure you want to remove yourself?' }
%></li>
                </ul>   
              <% else %>
                <%= link_to "Sign in with Facebook", "/auth/facebook/callback", id: "sign_in" %>
              <% end %>


              
          </li>
        </ul>
      </section>
    </nav>    
    <%= yield %>
    <%= javascript_include_tag "application" %>
     <script>
      $(document).foundation();
    </script> 
  </body>
</html>